import { TableFilterDirective } from './table-filter.directive';

describe('TableFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new TableFilterDirective();
    expect(directive).toBeTruthy();
  });
});
